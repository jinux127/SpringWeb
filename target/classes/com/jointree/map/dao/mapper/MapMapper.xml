<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jointree.map.dao.mapper.MapMapper">

	<select id="insertMapData" parameterType="HashMap">
		INSERT INTO
			trms.tb_map_test
			(map_lat, map_lng, map_address)
		VALUES(#{map_lat}, #{map_lng}, #{map_address});
	</select>
</mapper>